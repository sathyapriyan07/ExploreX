{
  "name": "explorex",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm:dev -w frontend\" \"npm:dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "lint": "npm run lint -w frontend",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

